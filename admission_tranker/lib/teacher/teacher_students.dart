import 'package:flutter/material.dart';

class TeacherStudents extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Students Data")),
      body: Center(child: Text("Teacher – Students List")),
    );
  }
}
