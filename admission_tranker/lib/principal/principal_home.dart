import 'package:flutter/material.dart';

class PrincipalHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Principal Home")),
      body: Center(child: Text("Welcome Principal")),
    );
  }
}
