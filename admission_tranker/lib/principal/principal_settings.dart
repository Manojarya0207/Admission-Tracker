import 'package:flutter/material.dart';

class PrincipalSettings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Settings")),
      body: Center(child: Text("Principal Settings")),
    );
  }
}
